import type { Element } from '../globals';

type menuitemAttributes = ['checked', 'command', 'default', 'disabled', 'icon', 'radiogroup', 'type'];

export type menuitemElement = Element<menuitemAttributes>;
